import { elementWrapper } from "../../framework/elementWrapper";

const chatBodyElement = document.createElement('div')

chatBodyElement.classList = ['w-full', 'h-full', 'rounded-[16px]', 'bg-slate-50'].join(' ');

export default chatBodyElement;